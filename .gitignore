# ── real roster: public-key -> identity map. NEVER commit. ──
users.json

# ── secrets ──
*.key
*.pem
*.htpasswd
.htpasswd
htpasswd
*_ed25519
*_ed25519.pub

# ── live data snapshots pushed by collectors ──
stats-*.json

# ── local-only overrides ──
config.local.*
*.local

# ── python ──
__pycache__/
*.pyc
